DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=content_generator

MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=content-generator
MINIO_USE_SSL=false

LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
# kosong = pakai LLM_MODEL
LLM_MODEL_CRITIC=

TTS_PROVIDER=edge
TTS_VOICE=id-ID-ArdiNeural
# hanya jika TTS_PROVIDER=openai
TTS_BASE_URL=
TTS_API_KEY=
TTS_MODEL=

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# password for the auto-seeded admin user (only used when the users table is empty;
# if unset, a random one is generated and printed once during migrate)
CG_ADMIN_PASSWORD=

PORT=8787
